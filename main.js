// Create an array "topic" and add 5 string values

// Try using a loop that appends a button for each string in the array.

// Create an on-click event that displays 10 corresponding ratings and images (gifs)

// Push button objects into the "topic" array